#include "main.h"
/**
 * print_alphabet - prints the alphabets in lowercase, followed by the new line.
 */
void print_alphabet(void)
{
char letter;
for (letter = 'a'; letter <= letter 'z'; letter++)
_putchar(letter);

putchar('\n');
}
